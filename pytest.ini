[pytest]
testpaths = tests
addopts = -v --tb=short --strict-markers
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
markers =
    slow: tests that take >5s (e.g. full model training)
